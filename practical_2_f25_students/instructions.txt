### DSC 360 F25: Practical Test 2

**Goal:** Complete these **four** small Python programs (**in any order**) within **60 minutes**:

- **chat.py** — Keep only the system prompt and the last **K** non-system turns before each model call.
- **prescreen.py** — Prompt-engineer a prescreener that classifies a prompt as `OK`, `INJECTION`, or `OFF_TOPIC`.
- **extract.py** — Extract a valid Centre course code (e.g., `CSC360`) or `None` using a Pydantic validator + short system prompt.
- **index.py + ask.py** — Implement the **embedding** component for a tiny Doc2Query RAG on Poe’s *The Tell-Tale Heart*.  
  1) Run `index.py` once (build questions + embeddings).  
  2) Then run `ask.py` (retrieve + answer from cited chunks).

#### What to do

- Read each header; implement the marked TODOs (small helpers/debug scaffolding OK).
- Don’t change provided data, I/O paths, or utilities.
- Run with `python3 filename.py` (`index.py` before `ask.py`).
- Submit the **five** completed `.py` files (optionally include your built index artifacts).

#### Timing

- In class: 60 minutes.  
- Take-home: until class time **Friday** to finish missed problems for up to **50%** of missed points.  
  - Don’t wait for feedback—get started. (Do read any feedback carefully before you resubmit.)

#### Scoring

- **Exponential completion:** your **first** correct problem = **40** pts, **second** = **30**, **third** = **20**, **fourth** = **10** (any order).

*Tip:* Many students will find **chat.py** fastest. The Doc2Query pair (**index.py + ask.py**) is mostly a tiny embedding function—lighter coding than **extract.py**—but the build/run takes a few minutes. Start anywhere and budget your time.

---

#### Submission

**Upload the correct, runnable versions of your files.**  

No credit for incomplete solutions (except as noted in `prescreen.py`). You may use the take-home window for partial recovery (up to 50% of missed points). After that, scores are final—shift focus to the Final Project.
